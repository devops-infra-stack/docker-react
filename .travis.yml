sudo: required
services: 
  - docker

before_install:
  - docker build -t subash/docker-react -f Dockerfile.dev .

script: # list of command that need to run our test suits
  - docker run -e CI=true subash/docker-react npm run test